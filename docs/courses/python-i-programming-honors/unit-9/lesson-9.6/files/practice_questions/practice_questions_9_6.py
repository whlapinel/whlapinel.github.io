# Practice Questions 9.6: String and Dictionary Operations
# For each problem, write a function that meets the requirements specified. Use the assert statements to test your code by uncommenting them.

# 1. 'only_vowels': Check if a string contains only vowels
# Accepts a string
# Returns a boolean

# assert only_vowels("aeiou") == True
# assert only_vowels("hello") == False

# 2. 'vowel_counts': Return a dictionary with counts of vowels in a string
# Accepts a string
# Returns a dictionary

# assert vowel_counts("hello") == {"e": 1, "o": 1}
# assert vowel_counts("xyz") == {}

# 3. 'unique_chars': Return the number of unique characters in a string
# Accepts a string
# Returns an integer

# assert unique_chars("hello") == 4
# assert unique_chars("") == 0

# 4. 'replace_spaces': Replace all spaces in a string with underscores
# Accepts a string
# Returns a string

# assert replace_spaces("hello world") == "hello_world"
# assert replace_spaces("") == ""

# 5. 'merge_and_sum': Merge two dictionaries and sum values of common keys
# Accepts two dictionaries
# Returns a dictionary

# assert merge_and_sum({"a": 1}, {"a": 2, "b": 3}) == {"a": 3, "b": 3}
# assert merge_and_sum({}, {}) == {}
